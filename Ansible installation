
update once
sudo apt-get update
install additional software properties
sudo apt-get install software-properties-common
add ppa repository for ansible
sudo apt-add-repository ppa:ansible/ansible -y
update once in this time local apt-repository will update
sudo apt-get update
type below command
sudo apt-get install ansible -y
edit the /etc/ansible/hosts
[ansible_server]                    //server ip
alias ansible_ssh_host=172.31.40.212
[clients]
host1 ansible_ssh_host=172.31.47.131  // host details

error
}
172.31.47.131 | FAILED! => {
    "changed": false, 
    "module_stderr": "Shared connection to 172.31.47.131 closed.\r\n", 
    "module_stdout": "/bin/sh: 1: /usr/bin/python: not found\r\n", 
    "msg": "MODULE FAILURE\nSee stdout/stderr for the exact error", 
    "rc": 127
    check pyhton2
    it lead to pyhton console
    Ubuntu 16.04 requires Python 2 installed for this playbook to run.

sudo apt-get install python-simplejson
ansible all -m ping
172.31.47.131 | SUCCESS => {
    "changed": false, 
    "ping": "pong"
}
